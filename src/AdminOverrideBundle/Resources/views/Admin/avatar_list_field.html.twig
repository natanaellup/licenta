{% extends 'SonataAdminBundle:CRUD:base_list_field.html.twig' %}

{% block field %}
    {% if value is not null %}
        <img src="{{ asset(value)| imagine_filter('admin_show_avatar') }}">
    {% endif %}
{% endblock %}
